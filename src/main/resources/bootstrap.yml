spring:
  application:
    name: blockchain-middleware
  profiles:
    active: dperis
  cloud:
   config:
    uri: https://blockchain-middleware-config.herokuapp.com/blockchain-middleware-configserver/
    failFast: true
    user: user
    password: password